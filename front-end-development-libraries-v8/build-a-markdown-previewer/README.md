# Ink & Render

A minimal React Markdown previewer built for the freeCodeCamp Front End Development Libraries certification project.

## Run locally

```bash
npm install
npm run dev
```

The editor uses GitHub-flavored Markdown through `marked` and updates the preview on every keystroke.
